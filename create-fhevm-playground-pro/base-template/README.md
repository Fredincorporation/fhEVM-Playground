# fhEVM Example Template

This is a minimal offline template used by `create-fhevm-playground-pro` to scaffold example projects.

Usage after scaffolding:

```bash
cd my-example
npm install
npm test
```
